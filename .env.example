# YouTube Data API v3 key.
# Create one at https://console.cloud.google.com/apis/credentials after
# enabling the "YouTube Data API v3" for your project.
YOUTUBE_API_KEY=

# The doctor's channel, identified one of two ways (only one is required):

# 1) Preferred — the channel ID (starts with "UC..."). Find it via
#    https://www.youtube.com/account_advanced while signed into the channel,
#    or by viewing the channel's page source for "channelId".
YOUTUBE_CHANNEL_ID=

# 2) Or the channel's public @handle, e.g. "DrExampleMD" (no "@").
#    Used only if YOUTUBE_CHANNEL_ID is not set.
YOUTUBE_CHANNEL_HANDLE=anthonychaffeemd

# Optional. Supadata (https://supadata.ai) API key for fetching video
# transcripts, so the model can reason about a video's actual spoken
# content rather than just its title/description/tags. Get one at
# https://dash.supadata.ai/organizations/api-key.
#
# Without this set, everything still works — search just has no
# transcripts to draw on. When set, only native (existing-caption)
# transcripts are fetched, never AI-generated ones, and only for the top
# few results of each search — see src/transcript.ts for why.
SUPADATA_API_KEY=

# Port the MCP server listens on locally.
PORT=3001
